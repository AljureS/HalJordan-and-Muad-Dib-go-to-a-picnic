---
description: Delegate a bounded task to Codex (HalJordan) and verify the result
argument-hint: <task brief path or one-line task>
---
Use the `codex-delegate` subagent for this task: $ARGUMENTS

Before dispatching, confirm the working tree is clean (or say why --allow-dirty is acceptable), and after it returns, summarize files changed, tests run against the exact commit/status, and what still needs the owner's decision.
